# UpdateTagInputBodyPrivacy

Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members.

## Example Usage

```go
import (
	"github.com/censys/censys-sdk-go/models/components"
)

value := components.UpdateTagInputBodyPrivacyPrivate
```


## Values

| Name                               | Value                              |
| ---------------------------------- | ---------------------------------- |
| `UpdateTagInputBodyPrivacyPrivate` | private                            |
| `UpdateTagInputBodyPrivacyShared`  | shared                             |